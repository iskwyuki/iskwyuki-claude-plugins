# pr-review-loop 実走計測と自動マージ昇格判断

Plans.md **3.1** の記録。実 PR で pr-review-loop（`/code-review` マルチエージェント + 反証検証パス → 修正 → 再レビュー）を回し、指摘数・棄却数・誤修正数を測って、**条件付き自動マージ**（CI green ＋ 検証済み Critical ゼロ ＋ 収束）への昇格可否を判断する。

計測: 2026-07-04 / モデル: Fable 5 / Opus 4.8（レビュー・検証とも）。

## 指標の定義

| 指標 | 定義 |
|---|---|
| 指摘数 | レビューエージェントが上げた指摘の総数（重複統合後・Critical/Warning のみ。Info/Good は除外） |
| 棄却数 | 反証検証パスで refuted となり統合レポートから除外した数（＝ recall 過剰を precision 側で回収した数） |
| 修正数 | 実際に修正した指摘数 |
| 見送り数 | confirmed だが medium/構造的/ローカル修正不能で見送った数 |
| **誤修正数** | 修正したが後に「不要・誤り」と判明し revert/再修正した数（＝ **false-fix**。自動マージ昇格の最重要指標） |

## 実走記録

### 実走 #1: iskwyuki-claude-plugins PR #7（docs: breezing 安全策）

- 変更種別: docs（新規 78 行中心）/ レビュー: Full 5 エージェント並列 + Lead 実機検証パス / 周回: 2 周で収束

| 周 | 指摘数(C/W) | 棄却 | 修正 | 見送り | 誤修正 |
|---|---|---|---|---|---|
| 1 周 | 11（C2/W9） | 1（R7: WT_ID 記述誤り指摘を実物照合で反証） | 6 | 3（medium） | 0 |
| 2 周 | 4（新規: merge commit 空出力・ref typo・glob 汚染・WT_ID 表現） | 0 | 4 | 1（R14: issue 文面所在） | 0 |

- 特記: 1 周の Critical 2 件はいずれも「機械検証コマンド自体の偽陰性」で、5/5 エージェントが独立検出（recall 設計が機能）。棄却 1 件は検証パスの precision 回復が働いた実例。

### 実走 #2: Antenna PR #65（chore: bootstrap 0.10.0 同期 + 品質ゲート）

- 変更種別: config/assets（.githooks / package.json / .claude）/ レビュー: Full 4 エージェント + Lead 実機検証 / 周回: 1 周 + 修正後 end-to-end 再検証で収束

| 周 | 指摘数(C/W) | 棄却 | 修正 | 見送り | 誤修正 |
|---|---|---|---|---|---|
| 1 周 | 4（C2/W2） | 0 | 3 | 1（R4: prepare 方式の構造的限界、ローカル修正不能） | 0 |

- 検出した Critical 2 件（exec-bit の tree mode 100644 でゲート無音 skip / prepare が Docker deps ステージで install 破壊）はいずれも**本番に出れば実害**があるもので、この環境の FS 777 表示に隠れていた。目視 diff では検出困難で、マルチエージェント + 実機検証の価値が最も出た回。
- 横断波及: 同型バグ（exec-bit・prepare）を既 push 済みの tech-blog / post-syncer にも展開修正。根本原因を配信元 bootstrap skill にも還元（PR #8、0.10.1）。
- 修正後 HEAD を silent-failure-hunter が sandbox で end-to-end 再検証（第 2 周相当）→ 誤修正 0 を確認。

### 実走 #3・#4（degraded: session 上限で Lead self-review に縮退）

15:00–16:00 JST 頃にレビューサブエージェントがセッション上限で起動不可となり、docs/config の小変更 2 件は Lead 自身の実機コマンド検証で self-review した。マルチエージェント実走ではないため参考記録とする。

| PR | 変更 | 指摘 | 修正 | 誤修正 | 備考 |
|---|---|---|---|---|---|
| #8 | bootstrap skill 強化（docs） | Lead 自己検出 1（冪等性ギャップ） | 1 | 0 | bash コマンドを実機実行で検証 |
| #9 | operations.md（docs） | 0 | 0 | 0 | 事実主張・リンクを実機照合 |

## 集計

| | 実走 #1 | 実走 #2 | 合計（full 2 件） |
|---|---|---|---|
| 指摘数（C/W） | 15 | 4 | 19 |
| 棄却数 | 1 | 0 | 1 |
| 修正数 | 10 | 3 | 13 |
| 見送り数 | 4 | 1 | 5 |
| **誤修正数** | **0** | **0** | **0** |

- **誤修正率 = 0 / 13 = 0%**（full 実走 2 件）。degraded 2 件を含めても 0 / 14 = 0%。

## 昇格判断（2026-07-04）

**判断: 条件付き自動マージへは昇格しない。マージは手動を維持する。**

誤修正率 0% は良好だが、以下の理由で自動マージの信頼性根拠としてはまだ不足:

1. **サンプルが docs/config に偏り**: full 実走 2 件はいずれも docs/config 変更で、false-fix リスクが最も高いアプリケーションロジックの実績がゼロ。
2. **full 実走が 2 件で DoD の 3 件に未達**: 3・4 件目は session 上限で self-review に縮退した。
3. **レビューの可用性が自動マージの前提だが、実際に落ちた**: session 上限でマルチエージェントが起動不可になる事象が本計測中に発生。「CI green ＋ 検証済み Critical ゼロ」を自動で判定するには、レビュー基盤が安定して回ることが前提になる。
4. **実走 #2 で本番実害の Critical を 2 件検出**: 自動マージ（＝レビューをゲートにしない運用）だったら壊れたビルドが出ていた。レビューの価値が高い＝レビューをスキップする自動マージのリスクが高い、という両面。

### 昇格の再評価条件

次のすべてを満たしたら再評価する:

- **アプリケーションコード**（docs/config でない）の PR で full マルチエージェント実走を **3 件以上**
- それらで**誤修正率が継続して 0% 近傍**（1 件でも false-fix が出たら閾値・修正条件を見直す）
- レビュー基盤（サブエージェント or 代替）が**安定して起動できる**運用の確立
- 昇格しても、初期は「自動マージ提案 → 人間が最終承認」の半自動から始め、full 自動化は段階的に

それまでは [operations.md](operations.md) のマージ方針どおり、pr-review-loop は収束後「マージ可能」と報告し、セルフマージ自己点検を経て**手動マージ**する。

## 関連

- [operations.md](operations.md) — マージ方針（手動維持の正）
- 防御原則: `assets/skills/pr-review-loop/SKILL.md`（最大 2 周・修正閾値・マージ禁止）
